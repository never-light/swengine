#pragma once

#include <QObject>

class Viewport : public QObject {
	Q_OBJECT
public:
	Viewport();
	//~Viewport();

public slots:
	//void initialize();
	//void resize();
	//void shutdown();
	//void render();
};