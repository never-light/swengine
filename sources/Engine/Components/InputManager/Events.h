#pragma once

#include <Engine/Components/Platform/Base/Input.h>

using KeyboardKey = sw::platform::base::KeyboardKey;
using KeyboardKeyState = sw::platform::base::KeyboardKeyState;
using MouseButton = sw::platform::base::MouseButton;
using MouseButtonState = sw::platform::base::MouseButtonState;
using MouseState = sw::platform::base::MouseState;
using CursorPosition = sw::platform::base::CursorPosition;