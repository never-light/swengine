#include "Vertex.h"

Vertex::Vertex(const vector3& position, const vector2& textureCoordinates, const vector3& normal)
	: position(position), normal(normal), textureCoordinates(textureCoordinates) 
{

}
